/**
 * Find `workspaceStorage/<id>/state.vscdb` whose `workspace.json` folder matches this project path.
 */
export declare function findWorkspaceStateDbForFolder(projectRootAbs: string): string | null;
